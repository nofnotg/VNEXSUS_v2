{
  "name": "@vnexus/domain",
  "version": "0.1.0",
  "private": true,
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "tsc --noEmit",
    "test": "vitest run --passWithNoTests \"src/**/*.test.ts\" \"../../apps/web/lib/server/services/*.test.ts\"",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@vnexus/shared": "workspace:*",
    "zod": "^4.1.5"
  }
}
